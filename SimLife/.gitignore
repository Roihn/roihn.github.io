# Build/release tooling + staged 720p clips (~200 MB each): NOT part of the
# deployed static site. Data JSON is generated here and committed; media goes to
# Hugging Face. Local preview reads _build/staging directly (kept on disk).
_build/

# Candidate-day data not shown in the picker (picker = 1457 + 1417).
# Regenerate any of these via _build/export_demo_day.py if you re-add the day.
data/day_1100.json
data/day_1197.json
data/day_1274.json
data/day_1347.json

# byproducts
*.pyc
__pycache__/
